# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
# Redução OTIMIZADA de recursos (ISSUE 12.B). Opt-in no AGP 8.13, padrão no 9.0 — é o item
# "A redução otimizada de recursos não está ativada" do painel de otimização do R8 no Console.
# Medido em 2026-07-29 no mesmo commit: -1,07 MiB / -11,6% no AAB.
#
# O risco é real e NÃO aparece no build: ela decide QUAIS recursos entram, então um recurso
# alcançado só indiretamente pode ser removido e falhar em RUNTIME. Aqui foi conferido que o
# app não faz nenhuma busca de recurso por nome (`getIdentifier`, `"@drawable/..."` em string):
# zero ocorrências em app/ e data/, ou seja toda referência é estática, via R, e o shrinker
# consegue vê-la. Por isso não há `res/raw/keep.xml`.
#
# Ainda assim: se voltar a aparecer um `getIdentifier`, ou um recurso referenciado só por
# string, declare-o num keep.xml ANTES de confiar nesta flag.
android.r8.optimizedResourceShrinking=true

# NÃO subimos o AGP para 9.0 (a outra recomendação do mesmo painel). Ele exige Gradle 9.x
# (estamos no 8.13) e é uma major com quebras que obrigariam a revalidar Kotlin 2.0.21/KSP,
# Room e Hilt. E não traria ganho: o 9.0 só torna PADRÃO a flag acima, que já está ligada.
# Bump é tarefa própria, não efeito colateral de fechar um aviso do Console.