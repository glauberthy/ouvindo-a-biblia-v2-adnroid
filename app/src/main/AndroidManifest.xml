<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!--
      tools:ignore MissingIntentFilterForMediaSearch: o app suporta Android Auto por
      NAVEGAÇÃO + playback por mediaId (onGetChildren/onGetItem/onAddMediaItems no
      PlaybackService). Busca por VOZ (MEDIA_PLAY_FROM_SEARCH) é backlog — não declaramos
      a capacidade para não anunciar algo que ainda não suportamos.
    -->
    <application
        android:name=".OuvindoBibliaApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OuvindoABiblia"
        tools:ignore="MissingIntentFilterForMediaSearch">

        <service
            android:name=".service.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            tools:ignore="ExportedService">

            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <!-- Compatibilidade com clientes MediaBrowserService (ex.: Android Auto/AAOS).
                     CATEGORY_DEFAULT: alguns hosts de mídia (AAOS) consultam os serviços com
                     MATCH_DEFAULT_ONLY, que exige esta categoria para o serviço ser listado. -->
                <action android:name="android.media.browse.MediaBrowserService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="br.app.ide.ouvindoabiblia.cast.CastOptionsProvider" />

        <!-- Android Auto (ISSUE 4.C): declara o app como app de mídia. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

    </application>

</manifest>